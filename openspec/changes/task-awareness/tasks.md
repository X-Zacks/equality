# 任务感知 — 实施任务清单

## 阶段划分

```
Phase 1: system-prompt.ts — 1 任务 — 追加任务感知规则块
Phase 2: 验证             — 黑盒场景测试
```

---

## Phase 1：追加任务感知规则块

### Task 1.1：在 `buildSystemPrompt()` 中追加任务感知规则

**文件**：`packages/core/src/agent/system-prompt.ts`

**位置**：Windows PowerShell 规则段落之后，Skills 索引块（`if (options?.skills?.length)`）之前。

**变更**：在基础 prompt 字符串拼接完成后、Skills 判断之前，追加任务感知规则块。

```typescript
// 追加任务感知规则（在 Skills 块之前）
prompt += `\n
## 任务感知规则

### 1. 执行前澄清
当用户请求存在关键歧义（同一请求有多种截然不同的执行方向）或缺少无法推断的必要信息时：
- 在调用任何工具之前，提出 1-2 个最关键的问题
- 只问无法通过工具自行查明的信息（目录结构、文件内容等可以直接用工具看）
- 如果可以合理推断或有明显默认解释，不要打断——在开头说明你的理解假设，直接执行

### 2. 执行前计划
当任务需要 3 个或更多工具调用步骤时，在调用第一个工具前输出执行计划：

📋 执行计划：
1. [第一步简述]
2. [第二步简述]
...

每步一行，简洁描述目标（不写工具名称和参数）。输出计划后立即开始执行，不等待用户确认。

### 3. 执行后摘要
当本次完成了 2 个或更多工具调用后，最终回复使用结构化摘要：

✅ 完成（或 ⚠️ 完成但有问题）

**做了什么**：[一句话描述核心任务]

**结果**：
- [关键结果，如路径、数量、关键数据]

**注意事项**（如有）：[问题或用户需要知道的事]

纯对话或只调用 1 个工具时，正常回答，不使用此格式。`
```

**验收**：调用 `buildSystemPrompt()` 后，返回字符串包含 `## 任务感知规则` 块，且位于 Skills 块之前。

- [ ] 1.1 在 `buildSystemPrompt()` 中追加任务感知规则块

---

## Phase 2：验证场景

> 启动 Core + 前端后，手动发送以下消息验证。

### 场景 2.1：澄清触发
- [ ] 发送"帮我整理一下项目"
- [ ] Agent 在调用任何工具前提出澄清问题
- [ ] 问题不超过 2 个

### 场景 2.2：澄清不触发（可探查）
- [ ] 发送"帮我列出当前目录的文件"
- [ ] Agent 直接调用工具，不询问

### 场景 2.3：澄清不触发（有合理默认）
- [ ] 发送"帮我分析这个项目的代码质量"
- [ ] Agent 在开头说明理解假设，直接开始执行

### 场景 2.4：执行前计划触发
- [ ] 发送"帮我分析一个 Excel 文件并生成 HTML 报告，文件在 C:/data/sales.xlsx"
- [ ] Agent 在第一个工具调用前输出 `📋 执行计划：`
- [ ] 计划后立即开始执行，不等待回复

### 场景 2.5：执行前计划不触发
- [ ] 发送"帮我查一下当前目录有哪些 .py 文件"
- [ ] Agent 直接执行，无计划输出

### 场景 2.6：执行后摘要触发
- [ ] 发送任何需要 2+ 工具调用的任务（如读文件 + 写文件）
- [ ] 最终回复以 `✅ 完成` 或 `⚠️ 完成` 开头
- [ ] 包含"做了什么"和"结果"字段
- [ ] 不包含逐步描述每个工具的流水账

### 场景 2.7：执行后摘要不触发（纯对话）
- [ ] 发送"Python 的 list 和 tuple 有什么区别"
- [ ] Agent 正常文字回答，不输出 `✅ 完成` 格式

---

## 变更文件汇总

| 文件 | 变更性质 | 任务 |
|---|---|---|
| `packages/core/src/agent/system-prompt.ts` | 追加约 25 行 prompt 规则 | 1.1 |
